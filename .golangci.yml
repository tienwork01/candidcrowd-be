version: "2"
linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gocritic
    - misspell
    - revive
  settings:
    revive:
      rules:
        - name: exported
          disabled: true
formatters:
  enable:
    - gofmt
    - goimports
run:
  timeout: 3m
